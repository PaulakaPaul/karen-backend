# Admin account configuration
ticket.checker.admin.username=admin
ticket.checker.admin.password=admin

# Datasource configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/drone_app
spring.datasource.username= droneAppUser
spring.datasource.password=123456987asd

# JPA Configuration
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
